- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig
    ~/.oh-my-zsh: oh-my-zsh 

- shell:
    - [git submodule update --init --recursive, Installing submodules]
